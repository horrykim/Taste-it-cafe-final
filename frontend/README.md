# Taste It Frontend

This is the React/Vite frontend for the Taste It Cafe Management System.

Supabase is the backend and database platform for the project. The `docs/` directory at the repository root is the project source of truth for requirements, architecture, security, and development rules.

Environment configuration uses `frontend/.env.local`.

Never commit secrets, database credentials, or service-role credentials.
